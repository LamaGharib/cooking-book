import styled from "styled-components";
import { Box } from "@material-ui/core";

export const NavHolder = styled(Box)`
  background-color: pink;
`;

export const Search = styled(Box)`
  display: flex;
  margin-right: 10px;
  align-items: flex-end;
`;
